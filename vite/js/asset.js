const assets = []

for(let i=0; i<8;i++) assets.push(`sprites/run/Run (${i+1}).png`)
for(let i=0; i<20;i++) assets.push(`sprites/girl/Run (${i+1}).png`)

assets.push("audio/bgm.mp3")
assets.push("audio/uwu.mp3")

export default assets